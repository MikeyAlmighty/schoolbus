import { Meta } from '@storybook/addon-docs/blocks'
import Flex from '../../components/flex'
import Box from '../../components/box'
import Text from '../../components/text'
import { elevations, elevationAliases } from '../../config/theme'

<Meta title='Theme/ Elevations' />

# Elevations/surfaces
A list of the available elevation properties. 
Can be accessed through the theme by using `theme.elevations.<elevation>`

<Flex flexDirection='column'>
  {elevationAliases.map(key => (
    <Flex
      key={key}
      justifyContent='space-between'
      alignItems='center'
      mb='2.5em' 
    >
      <Text fontWeight='extrabold'>
        theme.elevations.{key}
      </Text>
      <Box
        alignItems='center'
        elevation={key}
        p='1em'
        width='70%'
      >
        <Text textAlign='center'>{elevations[key]}</Text>
      </Box>
    </Flex>
  ))}
</Flex>